# Pinned to a verified-working set. Embeddings run on fastembed (ONNX) instead
# of torch/sentence-transformers, so the app is light enough for free hosting
# (Render's 512 MB tier) and starts fast.

# --- runtime ---
langchain==1.3.14
langchain-core==1.5.3
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-groq==1.1.3
groq==0.37.1
fastembed==0.8.0
faiss-cpu==1.15.0
rank-bm25==0.2.2
pydantic==2.13.4
pydantic-core==2.46.4
python-dotenv==1.2.2
fastapi==0.141.1
starlette==1.5.0
uvicorn[standard]==0.52.1
python-multipart==0.0.32
pypdf==6.15.0

# --- index build only (ingest.py / scripts), not needed to serve the app ---
pytesseract==0.3.13   # also needs the tesseract binary: brew install tesseract
reportlab==5.0.0
matplotlib==3.11.1
pillow==12.3.0
