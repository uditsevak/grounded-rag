# Pinned to a verified-working set (fresh-install + full stress test, 28/28).
# The deploy can't be rebuild-tested on every push, so these are exact pins to
# stop a later build from drifting into a breaking langchain/fastapi release.

# --- runtime ---
langchain==1.3.14
langchain-core==1.5.3
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-groq==1.1.3
groq==0.37.1
sentence-transformers==5.7.0
transformers==5.14.1
tokenizers==0.22.2
huggingface-hub==1.27.0
torch==2.13.0
faiss-cpu==1.15.0
rank-bm25==0.2.2
pydantic==2.13.4
pydantic-core==2.46.4
python-dotenv==1.2.2
fastapi==0.141.1
starlette==1.5.0
uvicorn[standard]==0.52.1
python-multipart==0.0.32
pypdf==6.15.0

# --- index build only (ingest.py / scripts), not needed to serve the app ---
pytesseract==0.3.13   # also needs the tesseract binary: brew install tesseract
reportlab==5.0.0
matplotlib==3.11.1
pillow==12.3.0
