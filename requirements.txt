# runtime
langchain
langchain-community
langchain-groq
sentence-transformers
faiss-cpu
pydantic
python-dotenv
rank-bm25
fastapi
uvicorn[standard]
python-multipart  # multipart form parsing for file uploads
pypdf             # PDF text extraction (ingest + uploaded docs)

# index build only (ingest.py / scripts) — not needed to serve the app
pytesseract  # also needs the tesseract binary: brew install tesseract
reportlab
matplotlib
pillow
